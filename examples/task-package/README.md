# Example Task Package

This directory shows the minimal package format expected by the MVP.

Files inside `input/` are task inputs.
The `manifest.yaml` file describes execution requirements and deliverables.
